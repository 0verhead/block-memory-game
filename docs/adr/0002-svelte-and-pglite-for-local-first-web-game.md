# Svelte And PGlite For Local-First Web Game

We will build the game as a Svelte + TypeScript + Vite web app and persist completed runs and rounds with PGlite in browser IndexedDB. Svelte keeps the timed gameplay loop simple and DOM-native, while PGlite gives the project real local-first history and analytics without requiring a backend for the first release.
